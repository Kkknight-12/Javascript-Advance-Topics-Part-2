/*
 * You can associate objects in two ways: implicitly and explicitly. Both
 * types of association allow one object to send messages to another (aka
 * delegation), but they behave a bit differently.
 * */